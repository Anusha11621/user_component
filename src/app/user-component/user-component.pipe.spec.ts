import { UserComponentPipe } from './user-component.pipe';

describe('UserComponentPipe', () => {
  it('create an instance', () => {
    const pipe = new UserComponentPipe();
    expect(pipe).toBeTruthy();
  });
});
