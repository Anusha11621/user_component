import { UserComponentsPipe } from './user-components.pipe';

describe('UserComponentsPipe', () => {
  it('create an instance', () => {
    const pipe = new UserComponentsPipe();
    expect(pipe).toBeTruthy();
  });
});
